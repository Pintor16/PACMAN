void setup() {
size(800,600);
}

void draw (){
 background (0); 
    for (int i=0; i<height; i= i +60) {
  stroke(255); 
  line(0, i, width, i); 
} 
 for (int i=0; i<width; i= i +60) { 
  stroke(255); 
  line(i, 0, i, height); 
} 
}
